server:
  port: 8080
spring:
  thymeleaf:
    cache: false # 关闭页面缓存
    encoding: UTF-8 # 模板编码
    prefix: classpath:/templates/ # 页面映射路径
    suffix: .html # 视图的后缀
    mode: HTML5 # 模板模式
# 日志
logging:
  config: classpath:log4j2.xml


